> DRAFT — historical. The live workflow is `.github/workflows/release.yml`.
> Kept only so older notes that point here still make sense; do not activate
> this file.

name: Build installers (draft — superseded by release.yml)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/release.yml instead"
